<% provide(:title, 'All users') %>
<h1>All users</h1>

<%if notice%>
    <p id="notice"><%= notice %></p>
<%end%>

<%= will_paginate %>

<ul class="users">
    <%=render @users%>
</ul>

<%= will_paginate %>
