<li id="<%=review.id%>">
  <span class="user">
    <%=link_to(image_tag(review.user.avatar.url(:small)),
               review.user,
               title: review.user.name)%>
  </span>
  <%=render partial: "reviews/review",
        object: review,
        as: :review%>
</li>