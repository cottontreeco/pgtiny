<%@user ||= current_user%>

<a href="<%= user_path(@user) %>">
  <%= gravatar_for @user, size:100 %>
</a>
<h1><%= @user.name %></h1>
<span><%= link_to "view my profile", @user %></span>
<span><b><%=pluralize(@user.microposts.count, "micropost")%></b></span>
